<h1> <%= @book.title %> </h1>
<h3>By <%= @book.author.full_name %></h3>

<%= link_to 'Delete', @book, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete '#{@book.title}' ?" } %>
